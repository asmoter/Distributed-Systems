akka {
  actor {
    provider = remote
    # provider = cluster

        serializers {
          proto = "akka.remote.serialization.ProtobufSerializer"
        }

        serialization-bindings {
          "PriceResponse" = proto
          "PriceRequest" = proto
        }
  }

  remote.artery {
    canonical {
      hostname = "127.0.0.1"
      port = 2551
    }
  }
}